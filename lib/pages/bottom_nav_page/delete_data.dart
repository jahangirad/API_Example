import 'package:flutter/material.dart';

class Delete_Data extends StatelessWidget {
  const Delete_Data({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
