import 'package:flutter/material.dart';

class Upload_Data extends StatelessWidget {
  const Upload_Data({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
